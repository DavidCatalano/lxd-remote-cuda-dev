# TODO: Create script to copy this file to ~/.tmux.conf for ubuntu user once LXD container is created.

# === CORE SETTINGS ===
# Terminal and color support
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# Start window and pane indices at 1
set -g base-index 1
setw -g pane-base-index 1

# Reduce escape delay for better responsiveness
set -sg escape-time 10

# === SPLIT PANES ===
unbind %
bind | split-window -h -c "#{pane_current_path}"
unbind '"'
bind _ split-window -v -c "#{pane_current_path}"

# === RELOAD CONFIG ===
unbind r
bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"

# === NAVIGATION & USABILITY ===
# Mouse support - click to focus panes, drag to resize
set -g mouse on

# Vi-style navigation in copy mode
setw -g mode-keys vi

# === WINDOW & PANE MANAGEMENT ===
# Create new window in current path
bind C new-window -c "#{pane_current_path}"

# Resize panes with hjkl (vim-style)
bind h resize-pane -L 5
bind j resize-pane -D 5
bind k resize-pane -U 5
bind l resize-pane -R 5

# Zoom/maximize pane toggle
bind -r m resize-pane -Z

# === COPY/PASTE ===
set -g set-clipboard on
unbind -T copy-mode-vi MouseDragEnd1Pane
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel
bind-key p paste-buffer